version: 2.1

jobs:
  upload-policies:
    docker:
      - image: cimg/node:24.5.0
    steps:
      - checkout
      - setup_remote_docker
      - run:
          name: Test policies
          command: npx cerbos compile .
      - run:
          name: Upload policies
          environment:
            CERBOS_HUB_STORE_ID: GF5IFA563D8K
          command: |
            npx cerbosctl hub store replace-files . --message="Policy upload from CircleCI"

workflows:
  build-and-upload:
    jobs:
      - upload-policies:
          filters:
            branches:
              only: main
